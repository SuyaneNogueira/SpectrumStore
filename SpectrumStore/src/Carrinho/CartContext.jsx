import { createContext } from 'react';

// Cria e exporta apenas o contexto
export const CartContext = createContext();

// Opcional: você pode exportar um valor padrão para o contexto
export default CartContext;