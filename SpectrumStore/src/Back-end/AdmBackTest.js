import { pool } from './db.js';


 {import('express').Application} app

export const defineRoutes = (app) => {

app.get (api/pedido, async (res,req) => {

    try {
        
    } catch (error) {
        
    }

});






};